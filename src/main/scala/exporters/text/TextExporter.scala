package exporters.text

import exporters.Exporter

/**
 * The `TextExporter` trait extends the `Exporter` trait, specializing it for exporting data of type `String`.
 */
trait TextExporter extends Exporter[String] {}
